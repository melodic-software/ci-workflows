name: claude-lane-outcome
description: >-
  Outcome reporter for the Claude lanes. On a successful action outcome it
  records review-failed=false and stops. On any other outcome it projects the
  last SDK result message from the execution file into safe, structured
  metadata (classify.cjs — the sole reader of the file's free-text fields;
  see its header for the projection and classification contract), emits the
  machine-readable `class=<token>` ::error annotation the downstream
  aggregator polls for, and records the failure class and detail for the
  marker-comment step. Runs entirely on the github-script Node runtime — no
  jq or other runner-image tool dependency.

inputs:
  outcome:
    description: >-
      The Claude action step's `outcome` (e.g. steps.claude-review.outcome).
      Anything other than "success" is treated as an infrastructure failure.
    required: true
  execution-file:
    description: >-
      The Claude action step's execution_file output. May be unset, missing,
      or unparsable — all degrade to failure class `other`.
    required: false
    default: ""
  lane:
    description: >-
      Human-readable lane name used in the annotation text, e.g.
      "Claude review" or "Claude security review".
    required: true

outputs:
  review-failed:
    description: '"true" on a genuine infrastructure failure, else "false".'
    value: ${{ steps.outcome.outputs.review_failed }}
  failure-class:
    description: "auth | rate-limit | overloaded | other (empty on success)."
    value: ${{ steps.outcome.outputs.failure_class }}
  review-detail:
    description: >-
      Compact JSON projection of the last SDK result message (safe fields
      only), or a parenthesized placeholder string (empty on success).
    value: ${{ steps.outcome.outputs.review_detail }}

runs:
  using: composite
  steps:
    - name: Report review outcome
      id: outcome
      uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
      env:
        LANE_OUTCOME: ${{ inputs.outcome }}
        EXECUTION_FILE: ${{ inputs.execution-file }}
        LANE_NAME: ${{ inputs.lane }}
        ACTION_PATH: ${{ github.action_path }}
      with:
        script: |
          const path = require("node:path");
          const { classifyExecutionFile } = require(
            path.join(process.env.ACTION_PATH, "classify.cjs"),
          );

          const outcome = process.env.LANE_OUTCOME ?? "";
          const lane = process.env.LANE_NAME ?? "Claude review";

          if (outcome === "success") {
            core.info(`${lane} completed. Findings (if any) are in the PR comments.`);
            core.setOutput("review_failed", "false");
            return;
          }

          const { reviewDetail, failureClass } = classifyExecutionFile(
            process.env.EXECUTION_FILE ?? "",
          );

          // `class=<token>` is a machine-readable term, not prose: this
          // annotation is the detection surface a receiver polls through the
          // check-run API, so keep it bare and greppable.
          core.error(
            `${lane} exited with: ${outcome} class=${failureClass} ` +
              "(infrastructure error, not a code-quality signal — e.g. usage limit, " +
              "OIDC failure, SDK crash, is_error result, or max-turns exhaustion). " +
              `Last SDK result: ${reviewDetail}`,
          );

          core.setOutput("failure_class", failureClass);
          core.setOutput("review_detail", reviewDetail);
          core.setOutput("review_failed", "true");
